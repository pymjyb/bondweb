export interface Investor {
  id: string;
  name: string;
  description: string;
  image?: string;
  keyData?: string;
  contact?: string;
  link?: string;
}

export interface Issuer {
  id: string;
  name: string;
  description: string;
  image?: string;
  keyData?: string;
  contact?: string;
  link?: string;
}

